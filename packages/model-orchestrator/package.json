{
  "name": "@agentmesh/model-orchestrator",
  "version": "2.0.0",
  "description": "Agent Mesh — Local + Cloud model aggregation and dynamic scheduling",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentmesh/core-types": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  }
}
