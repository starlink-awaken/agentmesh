{
  "name": "@agentmesh/server",
  "version": "2.0.0",
  "private": true,
  "description": "Agent Mesh — Unified server with MCP, HTTP, and process management",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentmesh/core-types": "workspace:*",
    "@agentmesh/model-orchestrator": "workspace:*",
    "@agentmesh/gateway": "workspace:*",
    "@agentmesh/toolkit": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  }
}
